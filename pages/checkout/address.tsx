import {ShopseLayout} from "../../components/layout";
import {AddressSection} from "../../components/sections";

const AddressPage = () => {
  return (
    <ShopseLayout title="Dirección" pageDescription="Confirmar dirección">
      <AddressSection />
    </ShopseLayout>
  );
};
export default AddressPage;
